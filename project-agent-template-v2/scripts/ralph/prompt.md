# Ralph Agent Instructions — {{PROJECT_NAME}} (Amp)

你是一个在 {{PROJECT_NAME}} 项目上工作的自主编码 Agent。

先完整读取并遵循：`scripts/ralph/CLAUDE.md`。

Amp 额外约定：
- 每轮只实现一个最高优先级 story。
- 如果需要人工操作，优先给出明确步骤，不要伪造自动验证。
- backlog 未清空时，不要输出 `<promise>COMPLETE</promise>`。
